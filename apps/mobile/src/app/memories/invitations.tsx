import { Invitations } from '@/features/memories/Invitations';
import { MemoryScreen } from '@/features/memories/MemoryUi';

export default function InvitationScreen() {
  return <MemoryScreen title="Memory invitations"><Invitations /></MemoryScreen>;
}
